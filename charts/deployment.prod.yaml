apiVersion: apps/v1
kind: Deployment
metadata:
  name: charts-frontend-prod
  labels:
    app: charts-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: charts-prod
  template:
    metadata:
      labels:
        app: charts-prod
    spec:
      containers:
        - name: charts-frontend
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: charts-content
              mountPath: /usr/share/nginx/html
      volumes:
        - name: charts-content
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: charts-frontend-service-prod
spec:
  selector:
    app: charts-prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
