# charts deployment folders

This directory contains environment-specific build outputs for the charts app:

- dev/
- UAT/
- prod/

Use the following commands to generate the deployment bundles:

- npm run build:dev
- npm run build:uat
- npm run build:prod
- npm run build:ovh

These folders are ready for OVH deployment.
